# Archived nested ATTACKDRO snapshot

- Origin: `notebooks/attackdro/` nested Git repository.
- Latest nested commit at archival: `48577cb`.
- Status: provenance only; not active source, configuration, or documentation.
- Scope: only files that were unique or byte-divergent from the repository root were retained, preserving their relative paths.

The byte-identical `HANDOFF.md`, `MACBOOK_PROMPT.md`, and `PC_PROMPT.md` files were not duplicated here because canonical archived copies already exist under `docs/archive/`.
