# NOTE: Do NOT add torch / torchvision here.
# PyTorch must be installed separately with the CUDA 12.8 (cu128) build so it
# supports the RTX 5070 Ti (Blackwell / sm_120). See SETUP.md.

numpy
pyyaml
tqdm
matplotlib
# Experiment tracking
wandb==0.28.0
# tensorboard  # optional alternative

# Strong evaluation attacks (APGD linf/l2/l1) — official AutoAttack.
# Installed from git (PyPI lags); reports version 0.1.
autoattack @ git+https://github.com/fra31/auto-attack.git
