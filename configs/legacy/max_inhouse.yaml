# =============================================================================
# max_inhouse.yaml — CARD-5: MAX in-house anchor (approved 2026-07-03).
# Tramèr-style hard per-sample worst-norm training under OUR recipe/attacks —
# the clean T->0 anchor for the T-axis story (locuslab MAX ckpt = 25.0 is a
# checkpoint artifact; C&H retrained MAX = 44.0 ‡ — see POSITIONING §3).
# ONLY change vs attackdro.yaml: objective = per_sample_max (hard argmax).
# NOTE: all 3 attack forwards still update BN (identical pipeline to
# per_sample_soft; only aggregation differs) — deliberate, it isolates the
# soft-vs-hard axis.
# =============================================================================
_base_: configs/legacy/attackdro.yaml

method: attackdro
run_name: max_inhouse

train:
  groupdro:
    objective: per_sample_max
